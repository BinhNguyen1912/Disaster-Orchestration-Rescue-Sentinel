$kind: http-request
name: GET /rescue-teams?provinceId=1&status=AVAILABLE
description: Get available rescue teams in a province
url: "{{baseUrl}}/rescue-teams?provinceId=1&status=AVAILABLE"
method: GET
headers:
  Authorization: Bearer {{accessToken}}
queryParams:
  provinceId: "1"
  status: AVAILABLE
order: 3000
